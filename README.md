# lisp

Copyright (c) 2025 Ronan MERIEN

This is a Lisp interpreter in Java for studies purposes.

gram_lisp.dat is the grammar of the language use by gbf program (grammar compiler) to compile into tab_lisp.dat (binary version) use by the Pseudo Java compiler via the Loader class.

Use the main class to run prgs/test_lisp.txt.

